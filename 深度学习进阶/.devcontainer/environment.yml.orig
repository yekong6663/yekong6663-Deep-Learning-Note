name: d2l
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      # 教材“安装”一节指定的 PyTorch 与 d2l 版本。
      - torch==1.12.0
      - torchvision==0.13.0
      - d2l==0.17.6
      - jupyter==1.0.0
      - notebook==6.4.12
      - ipykernel==6.15.2
